# Дорожная карта Cognitive Core v0.2+

## Текущее состояние (v0.2.0)

**31 Python-файл, ~2800 строк кода. Все 5 слоёв памяти. 69 тестов. 0 warnings.**

| Слой | Статус | Описание |
|------|--------|----------|
| L1 Raw Events | ✅ | Приём событий через API, 6-шаговый security pipeline |
| L2 Daily Buffers | ✅ | Куратор-фильтр + LLM-анализ, дедупликация по дате |
| L3 Master Knowledge | ✅ | Еженедельный синтез, реестр инструментов, арбитраж |
| L4 Snapshots | ✅ | Полные/дельта снапшоты в MinIO (S3), restore |
| L5 Audit | ✅ | Полный аудит-лог всех операций |
| OP Operative | ✅ | KNN-поиск, сессии в Redis с 24h TTL, feedback loop |
| LLM | ✅ | DeepSeek V4 Pro (основной), OpenAI (fallback) |
| Языки | ✅ | RU/EN/ZH промпты, автовыбор по SYSTEM_LANGUAGE |
| Безопасность | ✅ | API-ключи, rate-limit, sanitizer (SQL/JS/XSS/Shell) |

---

## Фаза 2: Стабилизация и мониторинг (ближайшие 2 недели)

### 2.1 Реальные эмбеддинги
- **Проблема**: DeepSeek не имеет embeddings API, всегда используется hash-fallback
- **Решение**: добавить OpenAI `text-embedding-3-small` или локальный Ollama `nomic-embed-text` для продакшн-качества KNN
- **Приоритет**: высокий — влияет на точность operative search
- **Статус**: ⏳ Хеш-эмбеддинг улучшен (триграммы + словесные биграммы), добавлено предупреждение. Для реальных эмбеддингов нужен `OPENAI_API_KEY` или `LOCAL_AI_ENABLED=true`

### 2.2 RediSearch Vector Search ✅
- ~~**Проблема**: индекс `idx:operative_vector` создаётся, но не используется — поиск идёт через Python~~
- ~~**Решение**: перенести KNN в Redis (`FT.SEARCH` с вектором), индексировать эмбеддинги при создании OP-записей~~
- **Статус**: ✅ `index_domain_vectors()` вызывается при weekly consolidation и monthly audit. RediSearch KNN — первичный путь поиска.

### 2.3 Метрики и health-check ✅
- ~~Prometheus-метрики: latency перцентили, успешность LLM-вызовов, размеры слоёв~~
- ~~Health-check эндпоинт с детализацией (сейчас отдаёт ok/error без метрик)~~
- **Статус**: ✅ Health: version, uptime, db_size_mb, llm stats, system info. Prometheus: HTTP latency, LLM calls, layer sizes, rate limits, audit events.

### 2.4 Логирование ✅
- ~~Структурированные логи (JSON) с trace-id~~
- ~~Ротация логов API-контейнера~~
- **Статус**: ✅ JSON-логи с trace_id через `log_event()`. Ротация через Docker/journald.

---

## Фаза 3: Интеграция и расширение (2-4 недели)

### 3.1 Агентский SDK ✅
- ~~Python-клиент `cognitive-client` для внешних агентов~~
- ~~Сессионный контекст: автоматический feedback loop при закрытии сессии~~
- **Статус**: ✅ v0.2.0: Async + Sync клиенты, ретраи с backoff, remember_batch, query, сессионный контекстный менеджер, tools CRUD, метрики, кастомные исключения

### 3.2 Локальный ИИ (Ollama) ✅
- ~~Конфигурация уже есть в `.env`, не подключена~~
- **Статус**: ✅ Полностью подключён. LLMClient и embedder используют Ollama при `LOCAL_AI_ENABLED=true`. Fallback-цепочка: primary → ollama → fallback.

### 3.3 A/B тестирование моделей ✅
- ~~Инфраструктура в конфиге (`LLM_DAILY_ANALYZER_B`, `LLM_AB_TRAFFIC_PERCENT`)~~
- ~~Не подключена — нужно встроить в `LLMClient`~~
- **Статус**: ✅ A/B для всех 6 функций. `/ab-stats` эндпоинт. Авто-трекинг успешности A vs B.

### 3.4 Дополнительные языки ✅
- ~~Добавить: `ja`, `ko`, `es`, `ar`, `pt`~~
- **Статус**: ✅ 8 языков: ru, en, zh, ja, ko, es, ar, pt

---

## Фаза 4: Продвинутая память (4-8 недель)

### 4.1 Граф знаний поверх L3
- Связи между знаниями: `prerequisite`, `contradicts`, `generalizes`, `specializes`
- Neo4j или pgRouting для графовых запросов
- Визуализация графа знаний

### 4.2 Контекстное окно с приоритизацией
- Умная сборка контекста для LLM: самые релевантные L3-записи + инструменты + недавние уроки
- Лимит токенов с адаптивным truncation

### 4.3 Активное обучение
- Автоматический поиск пробелов в знаниях (gaps)
- Целенаправленный сбор данных через агентов
- A/B верификация новых правил перед включением в L3

### 4.4 Кросс-доменный перенос
- Перенос паттернов между доменами (напр., уроки из "codegen" → "design")
- Эмбеддинг-анализ семантической близости доменов

---

## Фаза 5: Production-ready (8+ недель)

### 5.1 Мультитенантность
- Изоляция данных по организациям/проектам
- Квоты на количество событий, знаний, инструментов

### 5.2 Горизонтальное масштабирование
- Шардирование PostgreSQL
- Redis Cluster
- Очередь задач (Celery/Redis) вместо asyncio.create_task

### 5.3 Streaming и WebSocket
- SSE для real-time нотификаций о консолидации
- WebSocket для живого operative search

### 5.4 Админ-панель
- Дашборд: графики роста знаний, здоровье L3, активность агентов
- Визуальный арбитраж конфликтов
- Редактор L3 вручную

### 5.5 Бэкапы и disaster recovery
- Автоматические бэкапы PostgreSQL + MinIO
- Point-in-time recovery для L3

---

## Текущие баги и недоработки

1. ~~**Weekly consolidation — пустые домены**: worker.py берёт домены только из L3, новые домены без L3-записей никогда не попадают на weekly consolidation. Нужно брать домены из L2.~~ ✅ Исправлено: UNION L2+L3 уже в коде + добавлен недостающий import settings
2. **Embedder всегда fallback**: `embed_text()` всегда падает в `_hash_embed()` потому что DeepSeek не поддерживает `/embeddings`. Добавлено предупреждение при первом fallback. Для реальных эмбеддингов настроить `OPENAI_API_KEY` или `LOCAL_AI_ENABLED=true`.
3. ~~**Нет CORS**: API не отдаёт Access-Control-Allow-Origin — песочница не сможет работать из браузера~~ ✅ Исправлено: CORSMiddleware уже в main.py
4. ~~**Prompt user-message хардкод**: в analyzer.py user-сообщения ("Сырые данные:", "Проанализируй недельные буферы.") только на русском~~ ✅ Исправлено: все промпты в prompts.py на 8 языках
