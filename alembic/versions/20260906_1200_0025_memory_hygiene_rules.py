"""Seed 3 platform rules: гигиена записи в память (куда, что, чего не писать).

Фаза 5б (2026-09-06). Память копится, но пользы даёт меньше, чем могла бы:
запись уходит не в тот домен (тогда recall по домену возвращает пусто —
неотличимо от «памяти нет»), а в L1 попадает шум (heartbeat, «ок», пересказ
вопроса), который раздувает очередь и оплачивается LLM на консолидации.

Три правила рядом с существующим rule-memory-after-task (position 20):
куда писать (21), что достойно записи (22), чего не писать (23).

Add-only: только INSERT ... ON CONFLICT DO NOTHING, существующие правила
не трогаются. Идемпотентно — как 0011/0012.
"""

from alembic import op

# revision identifiers, used by Alembic
revision = "0025"
down_revision = "0024"
branch_labels = None
depends_on = None

RULE_IDS = (
    "rule-memory-domain-by-project",
    "rule-memory-worth-recording",
    "rule-memory-do-not-record",
)


def upgrade() -> None:
    op.execute("""
        INSERT INTO agent_rules (rule_id, severity, scope, lang, position, body, source, active)
        VALUES
        (
          'rule-memory-domain-by-project',
          'core',
          'post-task',
          'ru',
          21,
          'Домен записи в память — это ПРОЕКТ, а не тип события и не настроение. Используй уже живущие домены: crm (всё по AI-CRM), cognitive_core (сама платформа памяти, деплой, инфраструктура), design (интерфейсы, вёрстка, UX), work_journal (ход работ, договорённости, решения владельца). Новый домен заводи, только если работа реально не ложится ни в один из существующих. Промах по домену стоит дорого: recall по нему вернёт пусто, и это неотличимо от «в памяти ничего нет» — знание будет числиться записанным, но найти его не сможет никто. Не уверен в домене — пиши в work_journal, а не выдумывай новый.',
          'platform',
          TRUE
        ),
        (
          'rule-memory-worth-recording',
          'core',
          'post-task',
          'ru',
          22,
          'Записывай РЕЗУЛЬТАТ и ВЫВОД, а не ход работы. Хорошая запись отвечает на три вопроса: что сделал, чем это кончилось (с числами, если они есть), и что из этого следует на будущее. Ценность записи — в выводе, который в следующий раз сэкономит время: найденная первопричина, грабли и как их обойти, принятое владельцем решение и почему, рабочая команда или последовательность действий. Если через месяц запись не подскажет ничего, кроме «мы что-то делали», её не стоило писать.',
          'platform',
          TRUE
        ),
        (
          'rule-memory-do-not-record',
          'core',
          'post-task',
          'ru',
          23,
          'НЕ записывай в память: heartbeat и служебные пинги; подтверждения вроде «ок», «принял», «готов»; пересказ вопроса пользователя своими словами; промежуточные шаги, которые уже привели к записанному итогу; содержимое, которое и так есть в репозитории или в истории git. Каждая такая запись раздувает L1, оплачивается LLM на консолидации и разбавляет знание шумом — из-за неё хуже находится то, что действительно важно. Правило простое: нет вывода — нет записи.',
          'platform',
          TRUE
        )
        ON CONFLICT (owner_user_id, rule_id) DO NOTHING
    """)


def downgrade() -> None:
    op.execute("""
        DELETE FROM agent_rules
        WHERE owner_user_id IS NULL
          AND rule_id IN (
            'rule-memory-domain-by-project',
            'rule-memory-worth-recording',
            'rule-memory-do-not-record'
          )
    """)
